# UI

Angular + Monaco + xterm.js — AppShell ? MenuBar, ActivityBar, Sidebar (Explorer/Search/SC/Extensions), EditorArea (Tabs+Monaco), BottomPanel (Terminal/Output/Problems/Debug Console), StatusBar — dark first, minimal.

