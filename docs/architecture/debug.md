# Debug

DAP: Debug UI ? DebugService ? DAP Client ? Debug Adapter ? Debugger (GDB/LLDB/debugpy/Java). UI: start/stop, breakpoints, continue/pause, step over/into/out, variables, call stack, console.

