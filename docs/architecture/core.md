# Core

15 crates: ide-core, workspace, document, filesystem, process, platform, event-bus, command, configuration, language, lsp-client, build, debug, terminal, plugin

Each crate: public interface, minimal deps, unit test, docs, explicit direction. No circular deps.

